<ul class="nav">
  <li>
    <%= link_to "Postit!", root_path, :class => 'brand' %>
  </li>
  <li>
    <%= link_to 'link', '' %>
  </li>
</ul>
<div class='nav-item'>
  <%= link_to 'New Post', new_post_path, class: 'btn btn-success btn-small' %>

  <div class="btn-group">
    <a class="btn btn-info btn-small" href=<%= categories_path %> >Categories</a>
    <a class="btn btn-info btn-small dropdown-toggle" data-toggle="dropdown" href="#"><span class="caret"></span></a>
    <ul class="dropdown-menu">
      <%= Category.find_each do |category| %>
        <li><%= link_to category.name, category_path(category) %></li>
      <% end %>
      <li class="divider"></li>
      <li><%= link_to 'New Category', new_category_path %></li>
    </ul>
  </div>


  <!-- %= link_to 'New Category', new_category_path, class: 'btn btn-success btn-small' % -->
</div>
